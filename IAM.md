# IAM Notes

IAM users represent individuals.

Users can be assigned to groups.

IAM roles provide temporary credentials.

IAM policies are JSON documents that define permissions.

To follow best practices we should grant users the principles of least privilege.

We can audit IAM using tools such as: Credentials Report and Access Advisor(Last Accessed).
